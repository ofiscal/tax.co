# THIS IS NOT VALID JSON. (Json doesn't permit comments.)
# There should be a constraints-time-memory.json file, sibling to this.
# If its contents are one of the following bracketed items, it should work.

# For testing on the 1/1000 sample.
{ "min_survival_minutes" : 0,
  "max_user_gb" : 0.016,
  "max_gb" : 0.040
}

# MAYBE good parameters for production, with the full sample.
{ "min_survival_minutes" : 60,
  "max_user_gb" : 2.5,
  "max_gb" : 5
}
